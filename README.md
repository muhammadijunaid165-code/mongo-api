# AI-Powered Resume Screening & Interview Assistant

Monorepo containing:
- apps/backend (Node.js + Express + MongoDB)
- apps/ai-service (Python + Flask)
- apps/web (React + Tailwind)
- apps/mobile (Flutter)

See `/docs` for architecture and API references. Use `docker-compose up --build` for local stack.
